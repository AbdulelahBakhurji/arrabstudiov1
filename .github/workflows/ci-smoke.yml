name: CI smoke

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - ".github/workflows/ci-smoke.yml"

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm Actions can start a job
        run: echo "actions-ok"
